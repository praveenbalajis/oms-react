import styled from "styled-components";

export const StScrollSection = styled.main`
    max-width: 100vw;
    height: calc(100vh - 80px);
    overflow-y: auto;
    overflow-x: hidden;
    transition: 400ms;
`;